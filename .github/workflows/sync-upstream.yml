name: Sync upstream

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      # Real implementation pending — see SPEC.md §7 (`sync-upstream.yml`)
      # and §13 step 7. The script currently exits 64 (scaffold marker).
      - name: Check for newer upstream release
        run: ./scripts/refresh-upstream.sh || [ "$?" = "64" ]
