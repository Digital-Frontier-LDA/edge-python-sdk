#!/usr/bin/env bash
# refresh-upstream.sh — check PyPI for newer latitudesh-python-sdk and prepare a bump.
#
# Used by .github/workflows/sync-upstream.yml. Real implementation lands in step 7
# of SPEC.md §13.

set -euo pipefail

echo "refresh-upstream.sh: scaffold — implementation pending."
echo "See SPEC.md §7 (CI / release automation) for the design."
exit 64
